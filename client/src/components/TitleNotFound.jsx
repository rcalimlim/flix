import React from 'react';
import Container from 'react-bootstrap/Container';

const TitleNotFound = () => (
  <Container>
    <h1>Sorry, we couldn't find this title in Netflix's database.</h1>
  </Container>
);

export default TitleNotFound;
